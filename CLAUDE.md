# Tiramisu 3D: start here

A cozy 3D house game made by Amir Ariffin (Zetazuni) for Athirah. It is a 3D remake of the 2D Tiramisu App, with a camera that spins a full 360 degrees around the house. Models are made in Blender, the game is built in Unity.

If you are a new conversation or a new account, read these in order before doing anything:

1. **`docs/RULES.md`**: Amir's rules. Always follow them. (Short version: keep these notes updated, public repo so no secrets, everything on S:\, human wording with no em dashes, cinematic movie-like lighting and reflections, and **PC first**: high poly, detailed PBR textures, realistic physics, HDRP.)
2. **`docs/GAME_DESIGN.md`**: what the game is, what comes over from the 2D version and the phase plan.
3. **`docs/PIPELINE.md`**: how Blender and Unity connect through MCP, units, folders, naming and the asset workflow.
4. **`docs/devlog.md`**: what happened in each session and what is next. The last entry tells you where things stand.

## Quick facts

- Project folder: `S:\Tiramisu Corner by Zetazuni` (this Unity project root).
- Repo: https://github.com/zetazuni/tiramisu-3d (public, branch `main`, Git LFS for models, textures and audio).
- Unity 6000.6.3f1 with **HDRP 17.6** on DirectX 12, Blender 5.2.2 LTS, both driven by Claude through MCP. Target: Windows PC (dev PC has an RTX 4050 laptop GPU).
- The original 2D game is at `S:\Tiramisu App by Zetazuni`. Its `CLAUDE.md` is the detailed feature reference.
- Current version: **0.19.0** (decorate mode, day and night, pool ripples, upper floor furnished, cleaner car windows, small props on every surface, kitchen, bathroom and garage furnished, plus real trees and plants, photoscanned props, window frames, roof, graphics modes with DLSS, benchmark).
- New to Unity? `docs/UNITY_GUIDE.md` explains the panes, controls, testing and troubleshooting.
- Main scene: `Assets/Scenes/Main.unity`. It is generated, see "Greybox builder" below.

## Keeping these notes alive

- End of every session: add a devlog entry (what changed, how it was tested, what is next) and bump the version above if a release happened.
- If a rule, tool path, folder or decision changes, fix the doc that describes it in the same commit.
- Architecture notes for the Unity code go in a section below once there is code to describe.

## Architecture

One short paragraph per system. Keep these accurate as the code changes. All runtime code is in the `Tiramisu` namespace.

- **World coordinates:** 1 tile = 1 m. X runs along the house (0 living room end, 30 garage end), Z goes from the back wall (0) to the front glass (8) and on into the garden (up to about 22), so the garden is toward +Z. Ground floor top is y 0, the upper floor top is y 3.3 (`UPY`), walls are 3 m high, the slab under the house is 0.3 m and the garden ground sits at y -0.3. This matches the 2D game's grid (2D x = X, 2D y = Z) so furniture positions can be copied across.
- **Render setup:** HDRP 17.6 (`Assets/Settings/Tiramisu_HDRP.asset`, global settings in `Assets/HDRPDefaultResources`), DirectX 12 only, linear colour, legacy Input Manager (no Input System package; mouse, touch and keys all go through `UnityEngine.Input`). URP was removed in session 4.
- **House builder** (`Assets/Editor/GreyboxBuilder.cs`): menu **Tiramisu > Build greybox house** rebuilds `Main.unity` from scratch (house, garden, sun, volume, camera, furniture, physics). **Tiramisu > Set up render pipeline** makes and assigns the HDRP asset and the physics project settings. Change the numbers in the builder and rerun it, do not hand-edit the scene. `Wall()` builds a wall line with door gaps (and lintels above 2.3 m), `Room()` makes a floor plate, a `RoomMarker`, a light and a reflection probe. Architecture materials (`Assets/Art/Materials/Architecture`) come from `MakeMaterials()`.
- **Camera** (`Scripts/CameraRig/OrbitCamera.cs`): orbit around `pivot` with `yaw` (free 360), `pitch` (12 to 85) and `distance` (3 to 70). Input writes to private targets (`tPivot/tYaw/tPitch/tDist`) and the real values ease toward them every frame. Left drag spins (after a 6 px threshold, otherwise it counts as a click in `ClickedThisFrame` for picking furniture later), right or middle drag pans, wheel zooms; touch has one finger spin, two finger pinch, pan and twist. `FocusOn`, `FitHouse`, `SetPivotHeight` are what other scripts call. `IsOverUi` is set by the HUD so drags on buttons do not move the camera.
- **Floors and walls** (`Scripts/House/HouseView.cs`, `WallCutaway.cs`): `HouseView` has the view (Ground, Upper with the ground still visible below, Whole house with the roof) and the wall mode (Auto, Up, Down). Keys 1/2/3 switch floors, Tab cycles wall mode, F fits the house. Every straight wall line is a `WallCutaway` with its pieces as children; in Auto mode a wall cuts down to a 0.25 m stub when the camera and the focus point are on opposite sides of it. The focus is the camera pivot clamped inside the house footprint (`houseMin/houseMax`) so the front glass still cuts when you look from the garden. Whole house view keeps every wall up.
- **Rooms** (`Scripts/House/RoomMarker.cs`): one per room with display name, floor, size and order; the HUD lists the rooms of the active floor and the camera jumps to them.
- **Textures and materials** (`Assets/Editor/MaterialLibrary.cs`, `tools/fetch_textures.py`): 20 CC0 Poly Haven sets at 2K in `Assets/Art/Textures/<id>/` (colour jpg, OpenGL normal jpg, HDRP mask png packed as R metallic, G AO, B detail, A smoothness, plus a height png kept for later). `textures.json` holds each set's real size so it tiles at true scale. `Textured()` makes an HDRP/Lit material with Planar (floors), Triplanar (walls and blocks, so scaled cubes never stretch) or UV0 (Blender models, UVs in metres) mapping and a smoothness range; `Plain()`, `Glass()` (thin refraction, SSR on transparent) and `Water()` too. Every material goes through `HDMaterial.ValidateMaterial`. Run `python tools/fetch_textures.py` to add sets.
- **Film look** (`Assets/Editor/CinematicSetup.cs`, rules 5 and 6): `Pipeline()` turns on SSR (also on transparents), SSGI, SSAO, volumetrics and volumetric clouds, water, decals, ray tracing support, 4096 PCSS soft directional shadows, contact and screen space shadows, forces DX12. `PostVolume()` rebuilds `Assets/Settings/Cinematic.asset`: physically based sky with a cloud layer, volumetric fog, automatic histogram exposure (EV 7 to 15, +0.4), 4 cascade shadows to 90 m, contact shadows, SSR, SSGI, AO, ACES, bloom, grade, white balance, cool shadows and warm highlights, vignette, film grain, physical camera depth of field and light motion blur. Lights use physical units: the sun is 100000 lux at 6000 K, each room has a 900 lumen point light (2700 K, 4000 K in the garage and gym) and a box shaped baked HD reflection probe. Camera: physical 28 mm on a 36 x 24 sensor, f/2.8, TAA high. `Bake()` bakes the probes (no lightmaps). `Scripts/Graphics/CinematicFocus.cs` keeps focus at the orbit distance.
- **Physics** (`Assets/Editor/PhysicsSetup.cs`, `Scripts/Physics/PhysicsPoke.cs`): 90 Hz fixed step, 12/4 solver iterations. Physics materials in `Assets/Art/Physics` (Wood, Stone, Glass, Grass, Rubber, Metal, Fabric, Foliage) are matched to colliders by render material name (`AssignSurfaces`). Furniture: every part mesh gets a MeshCollider (convex when the piece can move) and the root gets a Rigidbody with a real mass from `Specs` (sofa 70 kg, table 38 kg, cushion 0.8 kg, rug static). Cushions are dropped from 0.9 m and settle on the sofa by themselves. `PhysicsPoke` shoves whatever movable thing you click, scaled by its mass (a test tool until decorate mode).
- **Furniture** (`Assets/Editor/FurnitureImport.cs` + the builder's `Layout` table): see `docs/PIPELINE.md`. Blender slot names map to textured materials in `FurnitureImport.Looks`. Pieces so far (high poly, one child per part): `sofa`, `marbletable`, `geomrug` (with tassels), `cushion` x2 in the living room, and the kitchen set (session 6, built by `tools/blender_kitchen.py` into `Blender/furniture_kitchen.blend`): `kitchenrun` (counter with sink, cooktop, hood, wall units and oven tower), `fridge`, `kitchenisland` (waterfall marble, walnut slats), `barstool` x3, `diningtable`, `diningchair` x4 and `pendant` x3 (glowing bulb plus a warm point light added by the builder). Bathroom and garage (`tools/blender_bathgarage.py` into `Blender/furniture_bathgarage.blend`, reuses the toolkit at the top of `blender_kitchen.py`): `shower`, `vanity`, `bathmirror` (attached to the back wall so it hides when the wall drops), `toilet`, `bathtub`, `towelrack`, `bathmat`, `washer`, `dryer`, `basket`, then `sedan` and `mpv` (lofted cars with wheel arches, glass and lights), `workbench`, `garageshelf`, `toolchest`, `bicycle`, `evcharger`. Small props (`tools/blender_props.py` into `Blender/props_home.blend`: `fruitbowl`, `cuttingboard`, `mug`, `espresso`, `utensils`, `herbs`, `towelstack`, `candles`, `cardboardboxes`, `paintcans`, `sparetyres`) are placed on surfaces from the builder's `Tabletop` table (id, x, surface height y, z, rotation) and are all real rigid bodies. Upper floor (`tools/blender_upper1.py` and `blender_upper2.py` into `Blender/furniture_upper1.blend` and `furniture_upper2.blend`): Teacher's Room (`platformbed`, `nightstand`, `bedlamp`, `wardrobe`, `bookcase` with hundreds of books, `teacherdesk`, `officechair`, `globe`, `chalkboard`, `worldmap`), Office & Library (`officedesk` with monitor, `filecabinet`, `uplight` with a warm light, `whiteboard`), Engineer's Room (`platformbed_e`, `ebench`, `robotarm`, `printer3d`, `beanbag`) and Gym (`treadmill`, `dumbbells`, `weightbench` with barbell, `spinbike`, `punchbag`, `yogamat`, `waterdispenser`, `gymmirror`). Upper floor items use `floor` 1 in `Layout`; `Tabletop` entries with y above 2 m go on the upper floor. `UpperProps` holds the Poly Haven chairs and plants up there. Wall pieces (chalkboard, worldmap, whiteboard, gym mirrors) are added to their wall's `WallCutaway.attachments` through `AttachTo`. Car windows are real edge loops in the cabin loft (glass and paint are picked per face, not by position). The shower is a fully glazed cubicle (front with a door, side and ceiling glass, no beam). Cars have glowing tail lights plus a red point light each. Cars and fixed fittings are static, small things (basket, tool chest, stools, chairs) are rigid bodies. New material names: `Cabinet_main`, `Stainless`, `Brass`, `GlassDark`, `Bulb`, `Ceramic`, `ClearGlass`, `CarGlass` (both use the glass material), `Mirror`, `Rattan`, `PaintRed`, `Car_main`, `CarSilver`, `Tire`, `Headlight`, `Taillight` (bulbs and car lights glow).
- **Photoscanned props** (`Assets/Editor/PropPlacer.cs`, `tools/fetch_models.py`): Poly Haven glTF models imported by glTFast. The builder's `LivingProps` and `GardenProps` tables place them (variant, position, rotation, scale, physics body, mass). See `docs/PIPELINE.md`.
- **Architecture details:** glass walls get black steel mullions (at most 1.5 m apart) and rails via `Frames()` in the builder, as children of the wall line so they cut away with it. The roof is flat with a deep front overhang, an oak soffit and a black steel fascia. `WallCutaway.attachments` hides things hung on a wall (the picture above the sofa) while that wall is cut down.
- **Graphics modes** (`Scripts/Graphics/GraphicsModes.cs`): Ultra (DLSS Quality, ray traced reflections, full resolution SSGI on high), Quality (default: DLSS Quality, screen space reflections, half resolution SSGI on low), Performance (DLSS Balanced, no SSGI, no volumetric fog, no contact shadows, 2 cascades). G key or the HUD button cycles, saved in PlayerPrefs. Falls back to native TAA without DLSS. DLSS needs `com.unity.modules.nvidia` plus dynamic resolution and DLSS switched on in the HDRP asset (`CinematicSetup.Pipeline`). Static foliage is excluded from ray tracing to keep Ultra affordable.
- **Benchmark** (`Scripts/Graphics/FpsBenchmark.cs`): F9 measures every mode and Quality with each effect off (2 s warm up, 6 s measure) and logs to the Console and `benchmark.txt` in `%USERPROFILE%/AppData/LocalLow/DefaultCompany/Tiramisu Corner by Zetazuni/`. Claude sets the Game view to 1920x1080 with `PlayModeWindow.SetCustomRenderingResolution` before measuring.
- **Version** (`Scripts/GameInfo.cs`): `Version` and `BuildDate`, shown bottom right by the HUD.
- **HUD** (`Scripts/UI/HouseHud.cs`): temporary IMGUI panel (floors, rooms, wall mode, fit, a hint line). Scales with screen height. Will be replaced by the real UI.
- **Decorate mode** (`Scripts/House/DecorateMode.cs`, `Furniture.cs`): key M or the HUD button. Every piece the builder places gets a `Furniture` component with a stable key ("sofa#0") and a `pinned` flag (built in fittings and wall pieces, see `Pinned` in the builder). Press on a piece to pick it up: it is made kinematic and follows whatever surface is under the mouse (raycast, walls and roof ignored), snapped to 5 cm (Ctrl = free), R turns it. Pieces standing on it (origin above its base and inside its footprint) ride along. `IsFree` checks an oriented box against everything except the surface it stands on and its riders, and a piece dropped in a wall goes back to the last free spot. The layout is saved to PlayerPrefs (`tiramisu.layout`, JSON, entries by key so new furniture does not break old saves) after every drop and loaded in `Start`. `OrbitCamera.Blocked` stops the camera from spinning while a piece is held, and `PhysicsPoke` is off in decorate mode.
- **Pool ripples** (`Scripts/Water/PoolRipples.cs`): the pool surface is a 98 x 49 grid mesh driven by a 60 Hz wave equation (reflecting walls, damping 0.985), with normals from the slope so screen space reflections show the rings. Falling things splash by impact speed, moving ones leave a wake, a click on the water drops a stone, and a breeze adds drips. Every rigid body in the pool gets Archimedes buoyancy from the bounding box of all its parts (`fillFactor` 0.4 of the box is solid) plus water drag, so cushions and tables float and heavy things sink slowly.
- **Day and night** (`Scripts/Graphics/DayNightCycle.cs`, `SwitchableLight.cs`): hour 0 to 24 (saved), sun elevation 68 degrees at noon, moon opposite it, a generated starfield cubemap as the sky's space emission, exposure limits open up at night (EV 4.2 minimum), reflection probes fall to 0.02 percent at night (they were baked in full daylight, at 10 percent every rough surface still glowed like it was sunny). Room lights are big soft rectangle panels in the ceiling (`RoomLightAndProbe`), pendants, downlights, lamps and pool lamps are small area lights too (`AddNightLight` with an area size), so nothing makes a hot spot. Bloom and exposure compensation drop a little at night. Lights that should change carry a `SwitchableLight` with a day and a night lumen value (room lights 500 to 1100, pendants, uplight, bedside lamps, path bollards, roof downlights, pool lamps). The time panel is top right in the HUD (slider, four presets, "Let time run" = 20 seconds per hour). Only one directional light casts shadows at a time, the sun wins.
- **Model orientation gotcha:** a Blender piece faces -Y, which arrives facing +Z, so it is turned half a turn. Blender +X therefore arrives as Unity -X (left and right swap when you place things by world x). The shower was wrong because of this. Check side-specific parts in Unity coordinates.
- **Modelling gotchas found by looking:** never put a door or panel with its front face exactly level with the carcass front, the two faces fight and flicker in diagonal stripes (the kitchen wall units did this, doors now stand 1 cm in front). Directional lights have `normalBias` 1.6 and `slopeBias` 1.0 to keep shadow acne away from thin panels. The money trees stand on the `planter` model (soil at 0.38 m), moving a planter in decorate mode carries its tree.
- **Sliding doors** (`Scripts/House/SlidingDoor.cs`, `DoorOpener.cs`, builder `SlidingDoorAt` and `BuildDoors`): every doorway between rooms (four on the ground floor, four upstairs) has a modern door hung on a black rail in front of the wall: framed glass in glass walls, walnut with fine grooves in concrete walls. `slide` says how far and which way the panel runs. A door opens when the mouse hovers over it (one raycast per frame for all doors), or when anything with a `DoorOpener` component is inside its sensor box (checked every 0.15 s), and shuts 1.2 s after the last one leaves. **People and pets: add `DoorOpener` to them and the doors open for them.** Doors are in their wall's `attachments`, so they hide with the wall when it is cut down.
- **Movable windows** (`Scripts/House/WindowWall.cs`, `WallWindowPart.cs`): the four concrete outer wall lines (back and left, both floors) are `WindowWall`s that build their own pieces around the door gaps and a list of windows (glass, slim black frame, oak sill) and rebuild when one moves. In decorate mode press on a window and drag it along its wall (10 cm grid, it cannot overlap another window, a door gap or the wall end), R changes the width (0.8, 1.2, 1.6, 2.2 m), Esc puts it back. Positions are saved in `tiramisu.windows`. `WallCutaway.Refresh()` re-collects the pieces after a rebuild.
- **Steady furniture** (`PhysicsSetup.Steady`): every movable body is locked upright (only turning on the spot is free), has heavy damping, and `PhysicsPoke` nudges sideways at 0.7 m/s times mass at most, so however often you poke a piece it only shuffles a few centimetres and never tips. **Reset layout** in the HUD (always visible) puts everything back upright.
- **Room lights** are now 2 to 4 soft cool panels per room (4300 K, 5200 K in the garage, gym, bathroom and office), see `RoomLightAndProbe`. There are about 64 area lights in the house, keep an eye on the frame rate if more are added.
- **Doors, all kinds** (`AutoDoor.cs` is the base, `SlidingDoor`, `HingedDoor`, `ShutterDoor` only decide how the door moves): hover or a `DoorOpener` in the sensor box opens any of them. Hinged pair of walnut doors in a black frame in the garden facade (living room, kitchen, bathroom, swinging out), and a roller shutter in the garage end wall (`GarageShutter`) that rolls up into a housing by squashing its slats towards the top edge, opening onto a driveway apron, a ramp and a road (`Road` in the builder, x 34.6 to 41). Doors in a wall hide with it when the wall is cut down (from the garden, the front doors are hidden while that wall is down). Upper floor doors live inside the upper floor group, so they hide with it.
- **Curtains** (`Curtain.cs`, built by `WindowWall.BuildCurtain`): two pleated panels on a black rod for every window. A click on the curtain or the window draws them shut or open (`PhysicsPoke`), open they bunch to 14 percent of their width. The state survives moving or resizing the window. Curtains are siblings of the wall line (not pieces) and are in its `attachments`. `WindowWall.Awake` picks up the ones built in the editor.
- **Sticky props** (`Scripts/Physics/StickyProp.cs`, `PhysicsSetup.MakeSticky`): every movable body under 3 kg (cushions, books, towels, mug, fruit, vases, lamps, herbs, candles) is kinematic and sits fast on its spot. A nudge (`PhysicsPoke`) shifts it 3 cm and tilts it 5 degrees, and it eases back in 200 ms. `Furniture.Place` re-anchors them, so carrying the sofa in decorate mode carries its cushions to a new home. Cushions no longer drop at start, they are placed on the seat (Tabletop entries).
- **Room paints** (`Paint()`, the `p...` materials in the builder): each room has its own colour (living sand, kitchen sage, hall soft white, bathroom aqua, garage cool grey, teacher blush, office blue grey, landing white, engineer slate, gym coral). Outer concrete walls are a plain outer skin plus a room paint layer per zone (`WindowWall.Zone`), interior walls are two half thickness boxes, one per room (`Paint2` before building the wall).
- **Gym light** is boosted 3.6x (`boost` in `RoomLightAndProbe`) and its rubber floor tint is lighter, the garage 1.3x.
- **Front yard** (`tools/blender_garden.py` into `Blender/furniture_garden.blend`, entries in the builder's `Tabletop` table with y -0.3 = lawn, -0.06 = deck): fountain, garden bench, gnome, flamingo, mailbox, planter boxes, flower beds, hammock, three loungers with a parasol, a beach ball that floats in the pool, a BBQ corner (counter with sink, grill with glowing coals, cooler), a telescope, a long teak dining table with benches on a rug, and a fire pit lounge (fire pit with a flickering light, L shaped sofa, rug, two lanterns). Everything except the fountain, mailbox, BBQ counter, flower beds and hammock is movable in decorate mode. `Layout` and `Tabletop` now use y = -999 as "on the floor" and any other y as an absolute height.
- **Night glow and light strips** (`NightGlow.cs`, builder `StringLights`, `LedStrips`): festoon string lights on six posts over the BBQ and dining corner (sagging cables, bulbs, a real soft light every fifth bulb) plus two strings up to the roof edge; LED strips under the roof edge, the upper floor slab, the deck edge, the slab base, every stair tread and around the pool rim. `NightGlow` gives a surface its own material copy and `DayNightCycle` scales its emission with the darkness, so they are dark by day. Roof and upper floor strips are children of the roof and the upper floor and hide with them. `Flicker.cs` makes the fire light flicker without compounding.
- **Garage front folding door** (`FoldingDoor.cs`): six glass panels hinged at the two sides that fold like an accordion (alternate panels turn the other way, up to 84 degrees) in the garage part of the front glass. Same hover and `DoorOpener` behaviour as the other doors. The two cars are parked in a row with their noses to the shutter door (east end).
- **Kitchen floor** is large format polished marble (Poly Haven `marble_01`, tinted soft warm white, 2 m tiles at real size).
- **Curtains** are now 60 to 90 overlapping round folds per panel that alternate in depth (8 cm), with a gathered heading.
- **Cushions on the sofa** floated because the layout used the old drop height (they were placed 10 to 14 cm above the seat). Now placed exactly on the seat.
- **Pool** (builder `BuildGarden`): black marble with white veins (Poly Haven `grey_cartago_03`, its neutral greyscale map tinted 0.55, the orange is dropped), a 3 x 2 m shallow jacuzzi attached to the far long edge (`Jacuzzi water` = its own `PoolRipples` with breeze 9, plus two `WaterJet.cs` jets: nozzles in the far wall shoot streaks up and over in a real parabola, each landing stirs the surface), and a wall fountain in black marble on the east short end (`FallingWater.cs` sheet of streaks that splash into the pool surface, warm and cool LED strips, an area light). Ground and lawn boxes have a hole for the tub, the pool far wall and coping are cut where the tub joins.
- **Round fountain** water is real (`SetupFountain`): three round `PoolRipples` (basin r 0.96, middle bowl 0.5, top bowl 0.27, `round` skips the grid corners, `buoyancy` off) plus three `FallingWater` rings (crown spray, top bowl overflow, middle bowl overflow) whose streaks splash the surface below. The water discs were removed from the Blender model.
- **Beach ball** (`RollingBall.cs`): sphere collider, 5 kg, free rotation. `PoolRipples` pushes it down the slope of the waves (3 x g x mass, clamped to 14 N) and rolls it, and it floats with its lowest 11 cm under water. Tested: it drifted 0.5 m and turned in 18 s.
- **Skylights** in the roof over the stairwell (light falls into the stair hall), the teacher's room, office, engineer's room and gym (`SlabWithHoles`, a curb, glass and a bar for each). The roof and soffit are built as grids of boxes around the holes.
- **Night:** moon 110 lux (was 14, still too dark outside), LED strips at least 5 cm thick and 18 x emission, night exposure floor EV 3.4.
- **Small things** are saved relative to the piece they rest on (`Furniture.HostOf`), and `StickyProp.Settle()` drops them onto whatever is under them after loading and after a shove, so nothing hangs in the air whatever an old saved layout says.
- Soil (`Soil` material, Poly Haven `forest_ground_06`, non reflective) in flower beds and planters. The lounger backrest was tilted the wrong way (upside down), fixed. `shrub_04` weeds are sunk 16 cm so they stand in the lawn.
- **Small things are part of what they sit on** (`Furniture.AttachSmallThings`, `StickyProp.Settle`, `DecorateMode.SettleWhenQuiet`): 1.2 s after Play starts (the heavy pieces push out of the floor a little at the start), every small thing (`SmallIds` in the builder, or anything with a `StickyProp`) is dropped onto the highest surface under five sample points of its footprint, so it touches with no gap, then parented to the piece under it (`attachedTo`) and stripped of its own body and `StickyProp`. Its colliders join the host's, it can never move relative to the sofa, table or counter, grabbing it in decorate mode grabs the host, and it is not saved on its own. Tested: cushions 0.583 and 0.585 on a 0.583 seat, and moving and turning the sofa kept the cushion offset identical to 5 decimals. Nudges no longer move small things.
- **Turning a piece:** hold the left button on it (dragging) and turn the mouse wheel, one degree per notch (`DecorateMode.Update`), R still turns 15 degrees. `OrbitCamera` ignores the wheel while `Blocked`, so it does not zoom meanwhile. The HUD turn buttons are gone (`RotateSelected` is left in the code, unused).
- **Plot and fence** (`BuildFence`, `FenceSide`, `FenceGate`): the whole plot x -3 to 34.25, z -5 to 23 is lawn (`SlabWithHoles` with holes for the pool and tub). A modern fence runs round it: black steel posts, walnut panels on a concrete plinth, a steel cap, a warm wall light on every post and a soft area light on every third. Each side is a `WallCutaway`, so the side between the camera and the house drops out of the way. Front gate at x 3.2 to 4.8 in line with the stepping stones, back gate at x 25.2 to 27 behind the garage, both double walnut gates that swing inwards and open like the doors (`HingedDoor`, hover, `DoorOpener`). The east side is open where the garage lane crosses it (z 1.3 to 6.9), with no gate.
- Weeds (`shrub_04`) removed, hammock moved to (14, 19.7), bathtub moved to (20.4, 2.0) beside the door to the garage.
- **Light flicker:** HDRP only draws 64 area lights on screen by default (`lightLoopSettings.maxAreaLightsOnScreen`), we have about 105, so lights popped in and out as the camera moved. `CinematicSetup.Pipeline` now sets 512 area and 2048 punctual lights. Watch this number if many more lights are added.
- **Carrying a piece fixes everything else:** in `DecorateMode.Grab` every other non kinematic rigidbody is made kinematic until the piece is put down, so nothing is pushed when it touches other things.
- **Sofa with its own seat cushions:** `Assets/Art/Models/sofa.fbx` was re-exported from `Blender/furniture_living.blend` with two `seat cushion` parts that rest exactly on the seat top (0.558 in Blender), so they are part of the sofa mesh and collider. The separate `cushion` entries are gone from the layout.
- **Back yard:** `tools/blender_backyard.py` (shed with a stocked interior and a hinged door built in Unity by `ShedDoor`, wheelbarrow, tool rack, log pile, watering can, hose reel), a paving path from the back gate to the house and along it to the shed (`BackPath`), planters, a bench, flower beds and lanterns.
- **Fountain bulbs:** ten strings of bulbs from a point above the fountain to a ring of 30 rim bulbs (`BulbLine`, `SetupFountain`).
- **Lane sliding gate** (`LaneSlidingGate`): a walnut slat gate in a steel frame across the garage lane, runs 5.9 m along the inside of the east fence, opens on hover or for anything with `DoorOpener`, hides with that fence side.
- **Colours:** outer walls are pastel brown (`wallBrown`), the edges of the floors (`slab`) and the fence plinths are dark brown. Vertical walnut beam screens (`VerticalBeams`, `BuildBeams`) sit 14 cm off the outer back and left walls in sets of 2 to 4 m where there are no windows, on both floors. Upper floor ones are inside the upper floor group.
- **Placeable area** in decorate mode is the whole plot inside the fence (x -2.7 to 33.9, z -4.7 to 22.7, `DecorateMode.Drag`).
- **Sofa cushions lean** 14 degrees against the back cushions (Blender, top edge rests 1.5 cm off the back cushion, bottom on the seat top), still part of the sofa mesh.
- **Shed** now has the house's roof (metal sheet on top, dark oak soffit, black steel fascia, `RoofMetal` look) and a string of bulbs along its four roof edges (`ShedLights`, `BulbLine`).
- **String light to the roof:** the far cable goes to the roof's corner (x 30.6) since the roof ends at x 30.7.
- **Hedges** (`Hedges`): a row of round bushes 0.55 m outside the fence on all four sides, open at the front gate (x 2.4 to 5.6), the back gate (24.5 to 27.7) and the garage lane (z 0.7 to 7.5). The **road moved out** to x 37.4 to 43.8 to make room for a **public sidewalk** (x 35.5 to 37.2) between the hedges and the road. The driveway apron runs to x 36.4 and a ramp drops to the road.
- **Outer wall lights** (`WallSconce`, `BuildWallLights`): modern black plates with two glowing slits and a soft light, 7 per floor on the back wall and 2 or 3 on the left wall. Upper floor ones live inside the upper floor, ground ones are always there, all hide with their wall.
- **Moon** is 55 lux (was 110). **Upper floor beams** were built by an older build in Amir's scene, a rebuild fixed it (checked that the group is hidden in the ground view).
- **Hedges** are clipped rectangular boxes (0.75 wide, 0.9 high, a narrower top layer, a few flowers), one long box per run between the gaps.
- **House sidewalk** (`HouseSidewalk`): a 1.2 m paved band round the back, left and right of the house (not across the garage apron), with a 1.8 m strip out to the back gate and a 1.6 m strip out to the front gate, ending in a pad inside each gate. The deck already covers the front of the house.
- **People and pets** (`Assets/Scripts/People`, models from `tools/blender_characters.py` into `Blender/characters.blend`): Athirah (hijab and abaya) and Amir, plus Miso the cat and Biscuit the dog, stylised figures (3 to 11k triangles). **Joints:** Blender's FBX axis conversion breaks joint hierarchies, so the FBX is flat and every part is named `joint|part`; `CharacterRig.BuildSkeleton` builds the joint hierarchy at runtime from tables (`PersonTable`, `CatTable`, `DogTable`, Blender positions turned into Unity space) and hangs the parts on it. Blender names repeated joints `pelvis.001`, `FindDeep` accepts that. `CharacterRig` poses by swinging joints about the figure's own right axis: Stand, Walk (stride follows the agent speed), Sit, Lie, Wave, Crouch (patting), Sleep, Groom, Happy (tail wag).
- **Walking** (`TiramisuNav`, `Character`): package `com.unity.ai.navigation` (kept as a local copy in `Packages/com.unity.ai.navigation` because the registry version does not compile on Unity 6.6, `GetInstanceID` is obsolete: two edits, `GetHashCode`, and its Tests folder removed). `TiramisuNav` bakes a NavMesh from the physics colliders 1.6 s after Play starts (all floors on, walls up, roof off for that moment) and again 1.2 s after any furniture was moved, with doors and gates left out of the bake (they open for whoever walks up), the stairs their own area (`StairsArea`) that pets are not allowed on. `Character` is a small state machine: idle, wander (people anywhere including upstairs, pets around the house), go to a `UseSpot` and sit or lie there, chat with the other person (they walk up, face each other, speech bubbles alternate), pat a pet (crouch, the pet wags), pets sit, groom and sleep. Weights: people sit 40, wander 22, chat 16, pat 14. `DoorOpener.All` is checked by every `AutoDoor` (no colliders needed), so every door, the garage shutter, the folding door and the gates open for them.
- **Places to sit or lie** (`UseSpot`, builder `AddSpots`): 28 spots on the sofa (3), armchairs, dining chairs, bar stools, office chairs, both beds, the beanbag, loungers, the outdoor sofa (3), garden bench (2), the long table (6) and the hammock. They are children of the furniture, so they move with it in decorate mode. Add a piece to `AddSpots` to make it usable.
- **HUD** (`CharacterHud`): name tags over everyone and speech bubbles. Anyone upstairs is hidden while the upper floor is off.
- **Outer sidewalk** (`OuterSidewalk`) replaces the band round the house: a 1.3 m paved ring outside the hedges (1.05 to 2.35 m outside the fence) that joins the road's sidewalk at x 35.5, open across the garage driveway, with strips through the hedge gaps to the front and back gates.
- **Cast:** Athirah, Amir and Bedah the calico cat (white with orange and black patches, black tail and one black ear). The dog is gone from the scene (model kept in `characters.blend`). Speech bubbles are white bold text on a dark box.
- **Online character models are not imported yet:** the Blender MCP Sketchfab and Poly Pizza integrations are switched off (they need an API key entered in the MCP panel in Blender). Rigged, faced people from there would also need retargeting onto `CharacterRig`'s joints.
- **Real character models (v0.19):** Athirah, Amir and Bedah are downloaded from Sketchfab (CC BY, credits in `Assets/Art/Models/Characters/CREDITS.txt`), prepared in Blender with `tools/blender_rig.py` (helpers: `build_armature`, `skin` = skin weights from the distance to each bone segment, because heat weighting fails on separate sculpted parts, `pose_neutral` and `pose_neutral_pairs` = bake a relaxed pose into mesh and rest pose, `export_character` = FBX plus a `.materials.json` of textures and colours) and `tools/blender_rig_athirah.py`. Athirah (hijab girl, 12.5k vertices) got a new 11 bone skeleton with our joint names, Amir came with a Base Human skeleton (his joint names are mapped in `CharacterRig.AmirBones`, T pose lowered to a relaxed stance), Bedah came with a cat rig (bones renamed to `body`, `head`, `tail`, `legFL`...) and was re-coloured as a calico (white, orange, black patches, `OrangeFur`). The FBXs are in `Assets/Art/Models/Characters/`; `FurnitureImport.ImportCharacters` builds an HDRP material for each entry of the json (texture or colour) and imports them with `Generic` animation type so the skin is kept. `CharacterRig` measures `RestHip` from the model and lifts the pelvis in world metres (imported skeletons are in centimetres). The old procedural figures (`blender_characters.py`) are no longer used.
- **Blender gotcha:** never run `bpy.ops.wm.read_factory_settings` through the MCP, it kills the add-on connection. Delete objects instead.
